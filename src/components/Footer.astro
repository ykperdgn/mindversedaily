---
const today = new Date();
---

<footer class="mv-footer">
  <div class="footer-content">
    <div class="footer-brand">
      <div class="footer-logo-container">
        <div class="footer-galaxy-container">
          <span class="footer-galaxy-emoji">🌌</span>
        </div>
        <div class="footer-brand-text">
          <span class="footer-mind-text">Mind</span><span class="footer-verse-text">Verse</span>
        </div>
      </div>
      <div class="footer-subtitle">Daily</div>
      <div class="copyright">&copy; {today.getFullYear()} MindVerse. Tüm hakları saklıdır.</div>
    </div>
    <div class="footer-links">
      <a href="https://twitter.com/MindVerseDaily" target="_blank">Twitter</a>
      <a href="/horoscope">Horoskop</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
      <a href="mailto:mindversedaily@gmail.com">İletişim</a>
      <a href="/privacy-policy">Privacy Policy</a>
      <a href="/gizlilik-politikasi">Gizlilik Politikası</a>
    </div>
    <div class="mv-footer-social">
      <a href="https://twitter.com/MindVerseDaily" target="_blank" aria-label="Twitter"><span>🐦</span></a>
    </div>
  </div>
</footer>

<style>
.mv-footer {
  background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
  color: #e2e8f0;
  padding: 3rem 2rem 2rem;
  margin-top: 4rem;
  border-top: 1px solid rgba(226, 232, 240, 0.1);
}

.footer-content {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  align-items: start;
}

.footer-brand {
  text-align: center;
}

.footer-logo-container {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
}

.footer-galaxy-container {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 35px;
  height: 35px;
  border-radius: 50%;
  background: linear-gradient(135deg, #1e1b4b 0%, #312e81 25%, #1e40af 50%, #7c3aed 75%, #ec4899 100%);
  border: 2px solid rgba(255, 255, 255, 0.2);
  box-shadow:
    0 0 15px rgba(124, 58, 237, 0.3),
    0 0 30px rgba(124, 58, 237, 0.1),
    inset 0 0 10px rgba(255, 255, 255, 0.1);
  transition: all 0.4s ease;
}

.footer-galaxy-emoji {
  font-size: 1.2rem;
  animation: spin 8s linear infinite;
  filter: drop-shadow(0 0 6px rgba(255, 255, 255, 0.6));
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.footer-brand-text {
  display: flex;
  flex-direction: column;
  line-height: 0.9;
}

.footer-mind-text {
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 50%, #ec4899 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  font-weight: 900;
  font-size: 1.1rem;
  letter-spacing: -0.02em;
}

.footer-verse-text {
  background: linear-gradient(135deg, #8b5cf6 0%, #ec4899 50%, #f59e0b 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  font-weight: 900;
  font-size: 1.1rem;
  letter-spacing: -0.02em;
}

.footer-subtitle {
  font-size: 0.8rem;
  font-weight: 600;
  color: #94a3b8;
  margin-bottom: 0.75rem;
}

.brand-text {
  font-size: 1.5rem;
  font-weight: 700;
  background: linear-gradient(135deg, #60a5fa 0%, #a78bfa 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 0.5rem;
}

.copyright {
  font-size: 0.9rem;
  color: #94a3b8;
}

.footer-links {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  align-items: center;
}

.footer-links a {
  color: #cbd5e1;
  text-decoration: none;
  font-weight: 500;
  transition: all 0.3s ease;
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
}

.footer-links a:hover {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
  transform: translateY(-2px);
}

.mv-footer-social {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  align-items: center;
}

.mv-footer-social a {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 3rem;
  height: 3rem;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  transition: all 0.3s ease;
  font-size: 1.5rem;
  backdrop-filter: blur(10px);
}

.mv-footer-social a:hover {
  background: rgba(96, 165, 250, 0.2);
  transform: translateY(-3px) scale(1.1);
  box-shadow: 0 8px 25px rgba(96, 165, 250, 0.3);
}

@media (max-width: 768px) {
  .mv-footer {
    padding: 2rem 1rem 1.5rem;
  }

  .footer-content {
    grid-template-columns: 1fr;
    gap: 1.5rem;
    text-align: center;
  }

  .footer-links {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
  }
}
</style>
