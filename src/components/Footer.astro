---
const today = new Date();
---

<footer class="mv-footer">
  <div class="footer-content">
    <div class="footer-brand">
      <img src="/favicon.svg" alt="MindVerse" class="footer-logo" />
      <div class="brand-text">MindVerse Daily</div>
      <div class="copyright">&copy; {today.getFullYear()} MindVerse. Tüm hakları saklıdır.</div>
    </div>
    <div class="footer-links">
      <a href="/about">Hakkımızda</a>
      <a href="/blog">Blog</a>
      <a href="mailto:contact@mindverse.com">İletişim</a>
    </div>
    <div class="mv-footer-social">
      <a href="https://twitter.com/" target="_blank" aria-label="Twitter"><span>🐦</span></a>
      <a href="https://github.com/" target="_blank" aria-label="GitHub"><span>💻</span></a>
      <a href="https://linkedin.com/" target="_blank" aria-label="LinkedIn"><span>💼</span></a>
    </div>
  </div>
</footer>

<style>
.mv-footer {
  background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
  color: #e2e8f0;
  padding: 3rem 2rem 2rem;
  margin-top: 4rem;
  border-top: 1px solid rgba(226, 232, 240, 0.1);
}

.footer-content {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  align-items: start;
}

.footer-brand {
  text-align: center;
}

.footer-logo {
  height: 3rem;
  width: 3rem;
  margin: 0 auto 1rem;
  opacity: 0.9;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  padding: 0.5rem;
}

.brand-text {
  font-size: 1.5rem;
  font-weight: 700;
  background: linear-gradient(135deg, #60a5fa 0%, #a78bfa 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 0.5rem;
}

.copyright {
  font-size: 0.9rem;
  color: #94a3b8;
}

.footer-links {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  align-items: center;
}

.footer-links a {
  color: #cbd5e1;
  text-decoration: none;
  font-weight: 500;
  transition: all 0.3s ease;
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
}

.footer-links a:hover {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
  transform: translateY(-2px);
}

.mv-footer-social {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  align-items: center;
}

.mv-footer-social a {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 3rem;
  height: 3rem;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  transition: all 0.3s ease;
  font-size: 1.5rem;
  backdrop-filter: blur(10px);
}

.mv-footer-social a:hover {
  background: rgba(96, 165, 250, 0.2);
  transform: translateY(-3px) scale(1.1);
  box-shadow: 0 8px 25px rgba(96, 165, 250, 0.3);
}

@media (max-width: 768px) {
  .mv-footer {
    padding: 2rem 1rem 1.5rem;
  }

  .footer-content {
    grid-template-columns: 1fr;
    gap: 1.5rem;
    text-align: center;
  }

  .footer-links {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
  }
}
</style>
