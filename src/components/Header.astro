---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';

const categories = [
  "health",
  "psychology",
  "history",
  "space",
  "quotes",
  "love"
];

const categoryIcons = {
  health: "🧬",
  psychology: "🧠",
  history: "📜",
  space: "🚀",
  quotes: "💬",
  love: "❤️"
};
---

<header class="sticky top-0 z-50 bg-white/95 backdrop-blur-md shadow-lg border-b border-blue-100">
  <div class="max-w-6xl mx-auto flex flex-col sm:flex-row items-center justify-between py-4 px-6 gap-4">
    <a href="/" class="flex items-center gap-4 font-extrabold text-3xl tracking-tight group">
      <div class="logo-container">
        <span class="galaxy-emoji">🌌</span>
      </div>
      <div class="brand-text">
        <span class="mind-text">Mind</span><span class="verse-text">Verse</span>
      </div>
    </a>
    <nav class="mv-categories">
      <a href="/blog" class="mv-category-tab">
        <span class="category-icon">📚</span>
        Tümü
      </a>
      {categories.map(cat => (
        <a href={`/blog/${cat}`} class="mv-category-tab">
          <span class="category-icon">{categoryIcons[cat]}</span>{cat.charAt(0).toUpperCase() + cat.slice(1)}
        </a>
      ))}
    </nav>
  </div>
</header>

<style>
  /* Logo ve Brand Styling */
  .logo-container {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, #1e1b4b 0%, #312e81 25%, #1e40af 50%, #7c3aed 75%, #ec4899 100%);
    border: 3px solid rgba(255, 255, 255, 0.3);
    box-shadow:
      0 0 30px rgba(124, 58, 237, 0.4),
      0 0 60px rgba(124, 58, 237, 0.2),
      inset 0 0 20px rgba(255, 255, 255, 0.1);
    transition: all 0.4s ease;
  }

  .galaxy-emoji {
    font-size: 2rem;
    animation: spin 8s linear infinite;
    filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.8));
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .group:hover .logo-container {
    transform: scale(1.1);
    box-shadow:
      0 0 40px rgba(124, 58, 237, 0.6),
      0 0 80px rgba(124, 58, 237, 0.3),
      inset 0 0 25px rgba(255, 255, 255, 0.2);
  }

  .group:hover .galaxy-emoji {
    animation-duration: 2s;
  }

  .brand-text {
    display: flex;
    flex-direction: column;
    line-height: 0.9;
  }

  .mind-text {
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 50%, #ec4899 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 900;
    font-size: 1.8rem;
    letter-spacing: -0.02em;
    text-shadow: 0 2px 10px rgba(59, 130, 246, 0.3);
  }

  .verse-text {
    background: linear-gradient(135deg, #8b5cf6 0%, #ec4899 50%, #f59e0b 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 900;
    font-size: 1.8rem;
    letter-spacing: -0.02em;
    text-shadow: 0 2px 10px rgba(139, 92, 246, 0.3);
  }

  .group:hover .mind-text,
  .group:hover .verse-text {
    animation: glow 2s ease-in-out infinite alternate;
  }

  @keyframes glow {
    from {
      filter: brightness(1) saturate(1);
    }
    to {
      filter: brightness(1.2) saturate(1.3);
    }
  }

  /* Kategori Tab Styling */
  .mv-category-tab {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    border-radius: 2rem;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    color: #2563eb;
    font-weight: 700;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    min-width: 80px;
    border: 2px solid rgba(37, 99, 235, 0.1);
    margin: 0 0.25rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    text-decoration: none;
    position: relative;
    overflow: hidden;
  }

  .mv-category-tab::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
    transition: left 0.3s;
    z-index: -1;
  }

  .mv-category-tab:hover::before {
    left: 0;
  }

  .mv-category-tab:hover {
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(37, 99, 235, 0.25);
    border-color: transparent;
  }

  .category-icon {
    font-size: 1.1rem;
  }

  @media (max-width: 768px) {
    .mv-categories {
      overflow-x: auto;
      padding-bottom: 0.5rem;
    }

    .mv-category-tab {
      font-size: 0.85rem;
      padding: 0.5rem 1rem;
      white-space: nowrap;
    }
  }
</style>

