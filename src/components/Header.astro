---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';

const categories = [
  "health",
  "psychology",
  "history",
  "space",
  "quotes",
  "love"
];

const categoryIcons = {
  health: "🧬",
  psychology: "🧠",
  history: "📜",
  space: "🚀",
  quotes: "💬",
  love: "❤️"
};
---

<header class="sticky top-0 z-50 bg-white/95 backdrop-blur-md shadow-lg border-b border-blue-100">
  <div class="max-w-6xl mx-auto flex flex-col sm:flex-row items-center justify-between py-4 px-6 gap-4">
    <a href="/" class="flex items-center gap-3 font-extrabold text-2xl tracking-tight">
      <img src="/favicon.svg" alt="MindVerse" class="h-12 w-12 rounded-full border-3 border-blue-200 shadow-lg bg-gradient-to-br from-blue-50 to-purple-50 p-1" />
      <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">MindVerse</span>
    </a>
    <nav class="mv-categories">
      <a href="/blog" class="mv-category-tab">
        <span class="category-icon">📚</span>
        Tümü
      </a>
      {categories.map(cat => (
        <a href={`/blog/${cat}`} class="mv-category-tab">
          <span class="category-icon">{categoryIcons[cat]}</span>{cat.charAt(0).toUpperCase() + cat.slice(1)}
        </a>
      ))}
    </nav>
  </div>
</header>

<style>
  .mv-category-tab {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    border-radius: 2rem;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    color: #2563eb;
    font-weight: 700;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    min-width: 80px;
    border: 2px solid rgba(37, 99, 235, 0.1);
    margin: 0 0.25rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    text-decoration: none;
    position: relative;
    overflow: hidden;
  }

  .mv-category-tab::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
    transition: left 0.3s;
    z-index: -1;
  }

  .mv-category-tab:hover::before {
    left: 0;
  }

  .mv-category-tab:hover {
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(37, 99, 235, 0.25);
    border-color: transparent;
  }

  .category-icon {
    font-size: 1.1rem;
  }

  @media (max-width: 768px) {
    .mv-categories {
      overflow-x: auto;
      padding-bottom: 0.5rem;
    }

    .mv-category-tab {
      font-size: 0.85rem;
      padding: 0.5rem 1rem;
      white-space: nowrap;
    }
  }
</style>

